{
  "license": "ISC",
  "scripts": {
    "lint:fix": "prettier */*.js \"*/**/*{.js,.ts}\" -w",
    "lint": "prettier */*.js \"*/**/*{.js,.ts}\" --check",
    "test:localnet": "EPHEMERAL_PROVIDER_ENDPOINT=http://localhost:7799 EPHEMERAL_WS_ENDPOINT=ws://localhost:7800 anchor test --provider.cluster localnet --skip-local-validator --skip-build --skip-deploy",
    "test:devnet": "anchor test --skip-local-validator",
    "build:deploy:localnet": "anchor build && anchor deploy --provider.cluster localnet",
    "build:deploy:devnet": "anchor build && anchor deploy --provider.cluster devnet",
    "build": "cd app && yarn run build",
    "ci": "cd app && yarn run ci",
    "dev": "cd app && yarn run dev",
    "format": "cd app && yarn run format",
    "format:check": "cd app && yarn run format:check",
    "lint-web": "cd app && yarn run lint",
    "start": "cd app && yarn run start"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@magicblock-labs/ephemeral-rollups-sdk": "^0.7.2",
    "@solana/web3.js": "^1.95.8"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^10.0.0",
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "typescript": "^5.7.3",
    "prettier": "^2.6.2"
  }
}
